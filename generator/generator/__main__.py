# __main__.py — entry point for the generator package
#
# Invoked via `python -m generator`. Fetches scored vulnerabilities from the
# API, renders the newsletter template, and delivers HTML and/or email output.

print("hello from generator")
